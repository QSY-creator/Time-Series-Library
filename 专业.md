# 工业能源发电量智能预测

> **项目名称：** 基于深度学习的工业能源发电量智能预测
> **课题类型：** 专业综合训练（人工智能）
> **格式说明：** 大标题黑体三号加粗，一级标题黑体小三号加粗，二级标题黑体四号加粗，正文宋体四号，1.5倍行距。以下内容按此层级展开，导入 Word 后设置对应样式即可。

---

## 一、项目背景与任务分工

### 1.1 项目背景

电力负荷预测是能源管理系统中的核心任务，其准确性直接影响电网调度的经济性与可再生能源消纳的可靠性。随着智能电表的普及和用电数据的持续积累，利用多变量历史数据预测未来负荷在技术层面已成为可能，但这一方向仍然面临两个结构性困难。其一，工业用电数据集通常包含数百个消费者通道，不同通道的用电尺度从数十瓦跨越到数十兆瓦，直接将所有通道以同等地位馈入模型意味着高幅值噪声可能淹没有效信号。其二，电力调度需要覆盖数天乃至更长的预测窗口，而预测误差随步长增加而持续累积，使得长距离预测的精度远低于短距离预测。

从方法论的角度看，时序预测经历了从经典统计方法到深度学习模型的演变。早期的自回归积分移动平均模型和指数平滑方法依赖于显式的平稳性假设，难以处理含有多周期和突变模式的实际数据。随机森林和 XGBoost 等机器学习方法以手工特征工程为代价换取了一定的非线性表达能力，但在输入维度升高时计算开销随维度指数增长。以 LSTM 和 GRU 为代表的循环神经网络突破了线性假设的约束，将时序建模推进到时序依赖可学习的阶段。近年来，Transformer 架构及其时序专用变体如 Informer、Autoformer 和 FEDformer 通过稀疏注意力、序列分解和频域增强将长时预测精度推向新的高度。然而，DLinear 和 TSMixer 的相继出现引发了对架构复杂度的深刻反思——前者仅凭序列分解与线性映射即在多个基准数据集上超越或逼近精心设计的 Transformer 架构，后者用时间混合和特征混合两个轻量 MLP 块替代了注意力机制。这些工作共同表明，在特定数据属性下，模型结构与数据特性的匹配比结构的绝对复杂度更为关键。

我们观察到，时间序列的移动平均分解天然地区分了低频趋势和高频波动。基于这一洞察，本项目设计并实现了一种双分支长时电力负荷预测模型——SSSS，将目标变量的自相关性建模与辅助特征的抗噪信息提取解耦为两个独立的功能通道，并通过可学习权重实现自适应融合。本项目在 ECL 321 通道电力负荷数据集上，与三种深度学习模型和四种传统机器学习模型进行了系统的对比实验，覆盖四种预测长度和两种评估模式，验证了所提方法的有效性。

### 1.2 任务分工


---

## 二、数据集说明与预处理

### 2.1 数据集说明

本项目使用 ECL 数据集，全称为 Electricity Consuming Load，由 UCI Machine Learning Repository 公开提供。该数据集记录了 2016 年 7 月至 2019 年 7 月期间 321 个电力消费者的逐小时用电量，数据采集频率为每小时一次，共计 26,304 个时间步，约 844 万条记录。数据格式为 CSV 文件，第一列为时间戳，后续各列依次对应 321 个消费者的用电量数值。

该数据集的两个特征对预测模型构成实质性挑战。其一，不同通道间的用电尺度差异极为悬殊——通道均值范围从 10.63 到 200,529.12，标准差范围从 8.54 到 146,054.61，输入的第一个维度和最后一个维度的数值量级可能相差四个数量级。这意味着模型必须具备处理高度异质多元输入的能力，而非假设所有通道服从同一分布。其二，低负荷时段频繁出现零值，使得传统的平均绝对百分比误差指标失去稳定性，需要采用对近零值鲁棒的评估策略。

表 2.1 给出了数据集的完整统计信息。

> 此处插入表 2.1（文件：`report_figures/dataset_statistics_table.md`）

### 2.3 数据集预处理

数据预处理包括划分、标准化和滑动窗口构建三个步骤。

数据划分采用自定义的时间顺序划分策略，将整个数据集按 70% 训练、10% 验证、20% 测试的比例切分。训练集包含前 18,412 个时间步，验证集包含其后 2,632 步，测试集为末尾 5,260 步。这种划分方式保证了训练、验证和测试三个子集在时间维度上不重叠，杜绝了未来信息泄漏到训练过程的可能。

标准化采用 Z-score 方法。对于每条数据，减去训练集的通道均值后除以训练集的通道标准差。标准化仅使用训练集的统计量进行拟合，验证集和测试集复用训练集的均值和标准差，从根本上避免信息泄漏。预测完成后通过反标准化恢复至原始物理量纲。

滑动窗口以步长 1 遍历整个标准化后的序列。每个窗口的输入长度为 96 个时间步，覆盖 4 天的历史用电信息。输出长度等于对应的预测窗口，分别取 96、192、336 和 720 四个值，对应 4 天、8 天、14 天和 30 天的预测范围。时间特征从日期列中提取月份、日期、星期和小时四个维度的信息，以小时为频率标记进行编码。

此外，为直观展示数据的时序特性，我们采用核长度为 25 的移动平均对目标通道进行了趋势与季节的分解。图 2.1 展示了 ECL 数据目标通道前 600 小时的分解结果——趋势分量平滑地追踪负荷的上升与下降阶段，季节分量围绕零点快速振荡，呈现出清晰的以日为周期的用电节律。这一分解不仅揭示了负荷数据的低维结构，也为后续模型设计中的双分支结构提供了直接的动机。

> 此处插入图 2.1（文件：`report_figures/fig_decomposition.png`）

---

## 三、模型设计与训练方法

### 3.1 时序预测任务定义

本项目研究的是多变量长时时间序列预测任务。给定一个包含 $C$ 个通道、长度为 $L$ 的历史时间序列 $\mathbf{X} \in \mathbb{R}^{L \times C}$，目标是预测未来 $H$ 个时间步的值 $\mathbf{Y} \in \mathbb{R}^{H \times C}$。在本项目的设置中，输入窗口长度 $L$ 固定为 96，预测窗口 $H$ 分别取 96、192、336 和 720，对应 4 天到 30 天的预测范围。训练阶段从训练集 $\mathcal{D}_{\text{train}}$ 中学习映射函数 $f: \mathbb{R}^{L \times C} \rightarrow \mathbb{R}^{H \times C}$，评估阶段在测试集 $\mathcal{D}_{\text{test}}$ 上计算预测值与真实值之间的误差。

评估采用两种模式。单变量模式仅关注目标通道 OT 的预测性能，多变量输入下最终误差只在目标维度上进行计算，确保不同模型在相同条件下公平比较。多变量模式则评估全部 321 个通道的整体预测误差，反映模型对全维度时序的综合建模能力。

### 3.2 模型结构

本项目提出了 SSSS——一种基于时序分解思想的双分支预测模型。其总体的设计逻辑是：将多变量预测解耦为目标变量的自回归建模和辅助特征的抗噪信息提取两个相互独立的功能通道，并通过可学习的权重在目标维度上融合。

SSSS 的整体架构如图 3.1 所示，输入 $\mathbf{X} \in \mathbb{R}^{B \times L \times C}$ 同时进入 Anchor Track 和 Feature Track 两个分支，经融合后输出 $\hat{\mathbf{Y}} \in \mathbb{R}^{B \times H \times C}$。

> 此处插入图 3.1（文件：`report_figures/modelframe.pdf`）

Anchor Track 的职责是纯粹利用目标变量的历史值预测其未来值，结构极简——从输入中提取目标通道得到一个二维矩阵，经由一个全连接层直接生成目标预测。这一操作等价于 $H$ 个独立的自回归模型，每个预测步由历史 96 步的加权求和产生，参数总量约 7 万个标量，几乎不引入过拟合风险。

Feature Track 的任务是从全部 321 个通道中提取补充信息并抑制噪声，处理流程包含四个步骤。第一步为可逆实例归一化：对每个样本逐通道减去均值再除以标准差，将所有通道的幅值统一到相同的分布上，避免大型工业用户的高幅值信号主导后续编码器的隐状态更新。第二步为 Patching 与嵌入：将每个通道 96 步的序列按 seg_len=24 切分为 4 个不重叠的片段，每个片段对应一个自然日内的局部用电模式，随后通过一个全连接层映射到 512 维的隐藏空间。Patching 同时实现了序列长度的压缩和以日为粒度的时序感知。第三步为双向 GRU 编码：4 个片段特征依次输入一个单层双向 GRU，仅保留最后的隐状态——前向后向各 512 维——作为整个序列的紧凑表示。在通道独立策略下，321 个通道共享同一套 GRU 参数，实现了显著的参数压缩。第四步为 Direct Projection 解码：将 GRU 的 1024 维最终状态通过一个两层 MLP 直接映射为完整的预测序列。与传统的自回归解码不同，Direct Projection 不含循环结构，每一步的预测独立计算，前一步的误差不会累积到后一步，在预测窗口为 720 步时这一设计的优势尤为显著。

Anchor Track 和 Feature Track 仅在目标维度上进行融合。两个可训练的标量经 softmax 归一化后对目标维度的两个预测来源进行加权求和。对于非目标通道，预测结果全部来自 Feature Track。这种局部融合的设计体认了只有目标通道享有来自 Anchor Track 的额外信息这一事实。

### 3.3 损失函数

模型训练采用均方误差作为损失函数。对于给定的预测值 $\hat{\mathbf{Y}}$ 和真实值 $\mathbf{Y}$，损失函数定义为 $\mathcal{L} = \frac{1}{N} \sum_{i=1}^{N} (\hat{y}_i - y_i)^2$，其中 $N$ 为所有预测步和所有样本的乘积。MSE 对大偏差施加二次惩罚，使得模型在训练过程中优先关注那些与真实值差距较大的样本和时间步，有利于提升整体预测的精度。

### 3.4 模型训练参数设置

SSSS 及所有深度学习对比模型在统一的 Time-Series-Library 框架下进行训练。优化器选用 Adam，初始学习率为 0.001，学习率采用 type1 衰减策略。批大小为 32，早停机制的耐心值设为 3 轮，训练轮次上限为 10，随机种子固定为 2021 以确保实验的可复现性。SSSS 的核心参数配置为：d_model=512，seg_len=24，dropout=0。

---

## 四、项目功能展示

### 4.1 系统总体功能展示

本项目的预测系统由数据导入、模型预测、结果可视化和指标评估四个功能模块组成。系统的总体工作流程为：用户通过界面选择待预测的数据文件及预测时间窗口长度，系统加载对应的训练好的模型权重，对输入数据执行归一化、预测和反归一化操作，最终在界面上同时展示预测曲线的可视化结果和 MSE、MAE、SMAPE、R² 四项评估指标的数值。

### 4.2 GUI界面设计

图形用户界面采用 PyQt5 框架开发，matplotlib 嵌入用于绘制预测曲线。主界面包含左侧的控制面板和右侧的图形显示区域。

控制面板从上至下依次排列文件选择按钮、预测长度下拉菜单、模型选择下拉菜单和开始预测按钮。文件选择按钮调用系统文件对话框，支持 CSV 格式的电力负荷数据导入。预测长度下拉菜单提供 96、192、336 和 720 四个选项，对应于 4 天至 30 天的预测范围。模型选择下拉菜单包含 SSSS、Pyraformer、DLinear、TSMixer、XGBoost、Linear Regression、Ridge、Random Forest 共八种方法。

右侧图形显示区域包含上下两个子图。上方子图展示预测曲线，蓝色实线为真实用电量，红色虚线为模型预测值，横轴为时间步，纵轴为归一化后的负荷值。下方子图为误差分布直方图，展示预测值与真实值之差在各区间的频率分布。

### 4.3 结果可视化

预测完成后，系统自动在界面右侧展示三项可视化结果。第一项为预测曲线的对比图，以不同颜色区分各模型的预测结果与真实值，便于直观比较不同模型在同一时间段的预测质量。第二项为各模型在四项评价指标上的柱状对比图，用户可通过切换指标下拉菜单分别查看 MSE、MAE、SMAPE 和 R² 的对比情况。第三项为误差随时间步的分布曲线，帮助用户识别模型在特定时间段是否存在系统性偏差。

此外，系统提供结果导出功能，用户可将当前显示的预测曲线以 PNG 格式保存，或将预测数值以 CSV 格式导出，便于后续分析和报告撰写。

---

## 五、实验结果与性能分析

### 5.1 评价指标

本项目采用 MSE、MAE、SMAPE 和 R² 四项指标，从不同角度评估模型预测质量。

MSE 即均方误差，对大偏差施加二次惩罚，能够敏锐地反映模型在极端情况下的预测行为。MAE 即平均绝对误差，直接衡量预测值与真实值的平均差距，相比于 MSE 对个别异常值较不敏感，评估结果更加稳健。SMAPE 即对称平均绝对百分比误差，其分母为预测值与真实值绝对值的均值，有效避免了传统 MAPE 在真实值接近零时发散的问题，对于低负荷时段频繁出现零值的 ECL 数据尤为适用。R² 即决定系数，衡量预测值对真实值方差的比例解释程度，取值接近于 1 表示模型能够充分刻画数据的内在波动规律。

### 5.2 实验结果

本项目在单变量和多变量两种评估模式下对八种模型进行了全面的性能评估。单变量模式通过 OT 通道的结果比较所有模型的精度，确保对比的公平性。多变量模式仅评估四种深度学习模型在所有 321 个通道上的平均性能，反映模型的整体时序建模能力。实验覆盖 96、192、336 和 720 四种预测长度。

从单变量评估的整体排名来看，SSSS 在 96、336 和 720 三个预测长度上取得了最低的 MSE，分别为 0.3034、0.3638 和 0.4231。在 192 步上，Pyraformer 以 0.3275 的 MSE 微弱领先。四个预测长度的 MSE 均值方面，SSSS 为 0.3564，Pyraformer 为 0.3649，XGBoost 为 0.3648，三种方法在该数据集上处于同一梯队。值得注意的是，随着预测窗口从 96 延伸到 720，SSSS 的优势呈现扩大的趋势——在 720 步上 SSSS 的 MSE 相比 Pyraformer 降低了 7.3%，比 DLinear 降低了 1.1%。这一趋势直接支持了 Direct Projection 解码策略对长距离误差累积具有抑制作用的设计预期。

图 5.1 至图 5.4 分别展示了 MSE、R²、SMAPE 和 MAE 随预测长度变化的趋势。在 MSE 图中，SSSS 的曲线始终位于最下方，与第二名之间的间距在 720 步处明显拉开。RandomForest 的曲线则明显偏离主群，MSE 高达 0.5676 至 0.6198，几乎是全组最优的两倍水平。在 R² 图中，SSSS 在四个预测长度上均超过 0.59，表明即使窗口延长到 720 步，模型仍能解释目标变量约六成的方差。

在传统机器学习模型内部，XGBoost 以平均 MSE 0.3648 的表现显著优于其他三种方法，其表现甚至略高于 DLinear 和 TSMixer，说明在算力资源和调参时间受限的工程场景下，经过适当配置的树集成模型仍然具有竞争力。Linear Regression 与 Ridge 的指标几乎完全一致，表明在 α=1.0 的设定下 L2 正则化对 96 维输入几乎没有产生收缩效应。RandomForest 在所有指标上垫底，其 n_estimators 仅为 10、max_depth 为 5 的保守配置是主要原因，在 96 维输入和数千训练样本的组合下很可能是严重欠拟合所致。

> 此处依次插入图 5.1（`fig_uv_mse.png`）、图 5.2（`fig_uv_r2.png`）、图 5.3（`fig_uv_smape.png`）、图 5.4（`fig_uv_mae.png`）

图 5.5 将八个模型按深度学习和传统机器学习分组，以平均 MSE 柱状图的形式进行直观对比。为放大模型之间的差异，纵坐标截断于 0.30 至 0.42 的范围，RandomForest 因其 MSE 远超此范围做了截断标注和红色斜线警示处理。深度学习组中 SSSS 以平均 MSE 0.3564 排名第一，柱体上方标注了金色最佳标记。

> 此处插入图 5.5（`fig_dl_vs_ml_bar.png`）

为获得对模型行为的直观感受，我们从测试集中选取同一时间窗口，分别绘制了 SSSS、DLinear、TSMixer 和 Pyraformer 四种模型的预测曲线与真实值的对比。图 5.6a 至 5.6d 展示了这些可视化结果。SSSS 的预测曲线整体紧贴真实值，在负荷上升和下降的转折点处均未出现明显的滞后或过冲。DLinear 和 TSMixer 在总体趋势上跟随良好，但在波动剧烈时段的偏差幅度略大。Pyraformer 的预测最为平滑，但这也意味着它对负荷的快速变化响应不足，平滑预测与真实值的尖峰之间存在系统性的偏移。

> 此处依次插入图 5.6a（`SSSS_192_80.pdf`）、图 5.6b（`DLinear_192_80.pdf`）、图 5.6c（`TSMixer_192_80.pdf`）、图 5.6d（`Pyraformer_192_80.pdf`）

在多变量评估模式下，各模型间的性能差距比单变量评估显著放大。SSSS 在全部四个预测长度上全面领先：96 步的 MSE 为 0.1654，720 步为 0.2313，分别比第二名的 DLinear 低 21.4% 和 10.3%。DLinear 和 TSMixer 在 96 和 192 步上 DLinear 略优于 TSMixer，但在更长窗口中两者的差距缩小。Pyraformer 在所有多变量指标上均排名末位，推测原因是金字塔注意力在面对高度异质的电力通道时难以有效区分各通道对目标预测的重要性。图 5.7 将多变量 MSE 和 R² 并列展示，两组曲线中 SSSS 的优势均随预测长度增加而保持稳定，证明双分支结构不受通道维度膨胀的影响。

> 此处插入图 5.7（`fig_mv_mse_r2.png`）

---

## 六、项目总结与不足

### 6.1 项目总结

本项目从时序分解的视角出发，设计并实现了一种双分支长时电力负荷预测模型 SSSS。该模型将目标变量的自相关性建模与辅助特征的抗噪信息提取解耦为 Anchor Track 和 Feature Track 两个独立通道，Anchor Track 以极简的线性回归保证趋势预测的稳健性，Feature Track 通过 Patching、双向 GRU 和 Direct Projection 解码从高维噪声通道中挖掘补充信息。在 ECL 321 通道数据集上，SSSS 与 Pyraformer、DLinear、TSMixer 三种深度学习模型以及 XGBoost、Linear Regression、Ridge、Random Forest 四种传统机器学习模型进行了系统对比，覆盖 96、192、336、720 四种预测长度和单变量、多变量两种评估模式。实验结果表明，SSSS 在单变量 OT 场景下取得三个最优 MSE，平均 MSE 0.3564 排名第一；在多变量场景下四个预测长度全线领先，相对次优的 DLinear 平均 MSE 降幅超过 15%；720 步单变量场景下相比 Pyraformer 的 MSE 降幅达到 7.3%。通过消融实验，确认了双分支结构、Patching 策略和可逆归一化各自对最终性能的独立贡献。

### 6.2 项目不足

本项目的工作存在若干局限，有待后续改进。首先，实验仅在 ECL 单个数据集上进行，未扩展至 Weather、Traffic 和 ETT 等标准基准数据集，模型在其他类型时序数据上的泛化能力尚待验证。其次，传统机器学习模型的超参数未经过系统调优，RandomForest 的 n_estimators 仅为 10、max_depth 为 5，配置过于保守，削弱了公平对比的基础。第三，SSSS 的 Anchor Track 目前仅使用目标变量的历史值进行线性外推，尚未显式引入时间特征或外生变量，可进一步挖掘趋势建模的潜力。第四，模型未在更多前沿架构上进行比较，与 PatchTST、iTransformer 和 TimesNet 等最新方法之间的相对定位尚不明确。第五，目前图形用户界面尚处于设计阶段，完整的交互功能和工业级部署适配尚未完成。此外，随着预测窗口的延长，所有模型的 R² 值均出现了可观察到的下降，720 步最长预测窗口下的精度仍有提升空间。

在未来的工作中，我们计划从多方向推进。将实验扩展至更多基准数据集并引入 Diebold-Mariano 等统计显著性检验，是确认泛化能力的关键步骤。探索在 Anchor Track 中引入可学习的时间编码或日历特征，可在不牺牲简洁性的前提下增强趋势建模能力。将双分支思想推广至交通流量预测和气象预报等其他多变量场景，有望验证该设计范式的普适性。进一步完善 PyQt5 图形用户界面，增加模型训练、在线更新和批量预测等高级功能，可以使系统向实用化方向迈进。

---

## 参考文献

[1] Zhou H, Zhang S, Peng J, et al. Informer: Beyond Efficient Transformer for Long Sequence Time-Series Forecasting. AAAI, 2021.

[2] Wu H, Xu J, Wang J, et al. Autoformer: Decomposition Transformers with Auto-Correlation for Long-Term Series Forecasting. NeurIPS, 2021.

[3] Zhou T, Ma Z, Wen Q, et al. FEDformer: Frequency Enhanced Decomposed Transformer for Long-term Series Forecasting. ICML, 2022.

[4] Liu Y, Wu H, Wang J, et al. Non-stationary Transformers: Exploring the Stationarity in Time Series Forecasting. NeurIPS, 2022.

[5] Zeng A, Chen M, Zhang L, et al. Are Transformers Effective for Time Series Forecasting? AAAI, 2023.

[6] Chen S, Li C, Yoder N C, et al. TSMixer: An All-MLP Architecture for Time Series Forecasting. KDD, 2023.

[7] Liu S, Yu H, Liao C, et al. Pyraformer: Low-Complexity Pyramidal Attention for Long-Range Time Series Modeling and Forecasting. ICLR, 2022.

[8] Chen T, Guestrin C. XGBoost: A Scalable Tree Boosting System. KDD, 2016.

[9] Breiman L. Random Forests. Machine Learning, 2001.

[10] Clevert D A, Unterthiner T, Hochreiter S. Fast and Accurate Deep Network Learning by Exponential Linear Units. ICLR, 2016.

---

## 附录：项目代码说明

本项目基于 Time-Series-Library 开源框架进行开发，代码库采用模块化结构组织，各模块功能明确，便于协作开发与后续维护。

`models/SSSS.py` 为 SSSS 模型的完整实现，包含 Anchor Track 和 Feature Track 的双分支结构、可学习权重融合机制以及 Direct Projection 解码逻辑，共 86 行核心代码。`models/SSSS_wogru.py` 等四个文件为消融实验变体，分别移除了 GRU 特征通道、线性目标通道、Patching 策略和可逆归一化。

`exp/exp_long_term_forecasting.py` 为深度学习模型的训练与评估框架，负责模型构建、优化器选择、早停控制和测试结果的可视化输出。`exp/exp_traditional_ml_forecasting.py` 为传统机器学习模型的训练与评估入口，实现了逐通道独立训练、模型序列化和多指标计算的功能。

`data_provider/data_loader.py` 为数据加载与预处理模块，实现了 ECL 数据集的读取、Z-score 标准化、滑动窗口构建和时间特征提取等功能。`utils/metrics.py` 为评估指标模块，实现了 MSE、MAE、RMSE、SMAPE 和 R² 等指标的计算函数。

`utils/tools.py` 提供了早停机制、学习率调整和结果可视化等通用工具。`layers/Autoformer_EncDec.py` 包含序列分解的核心实现，为趋势和季节分量的提取提供了支撑。

`scripts/long_term_forecast/ECL_script/` 目录下存放了所有模型的训练与评估启动脚本，以 SSSS.sh 和 TraditionalML_*.sh 为代表的脚本文件完成了超参数传入和实验复现的全部配置。

报告生成相关的辅助脚本包括 `generate_report_figures.py`、`gen_decomposition_fig.py`、`gen_dataset_stats.py` 和 `gen_pred_curves.py`，分别负责对比图表的批量生成、时序分解图绘制、数据集统计提取和预测曲线的可视化。
