# 基于时序分解的双分支长时电力负荷预测

## 摘要

多变量长时电力负荷预测面临高维噪声干扰与长距离误差累积的双重挑战。本文从时序分解的视角出发，提出了一种双分支预测模型 SSSS，将目标变量的自相关性建模与辅助特征的抗噪信息提取解耦为 Anchor Track 和 Feature Track 两个独立通道，并通过可学习权重实现自适应融合。Anchor Track 以纯线性层捕捉目标变量的自回归规律，Feature Track 通过 Patching、双向 GRU 和 Direct Projection 解码从所有通道的波动中提取补充信息，在机制层面规避了递归解码的误差累积。在 ECL 321 通道电力负荷数据集上，SSSS 与 Pyraformer、DLinear、TSMixer 三种深度学习模型以及 XGBoost、Linear Regression、Ridge、Random Forest 四种传统机器学习模型进行了系统对比，覆盖 96、192、336、720 四种预测长度以及单变量与多变量两种评估模式。实验表明，SSSS 在单变量 OT 场景下取得三个最优 MSE，平均 MSE 0.3564 排名第一；在多变量场景下四个预测长度全线领先，相对次优的 DLinear 平均 MSE 降幅超过 15%；720 步单变量场景下相比 Pyraformer 的 MSE 降幅达到 7.3%。消融实验确认了双分支结构、Patching 策略和可逆归一化各自对最终性能的独立贡献。

---

## 1 引言

电力负荷预测是能源管理系统中的核心任务，其准确性直接影响电网调度的经济性与可再生能源消纳的可靠性。近年来，智能电表的普及和用电数据的持续积累使得利用多变量历史数据预测未来负荷成为可能。然而，这一方向仍面临两个结构性的困难。其一，工业用电数据集通常包含数百个消费者通道，不同通道的用电尺度从数十瓦跨越到数十兆瓦，直接将所有通道以同等地位馈入模型意味着高幅值噪声可能淹没有效信号。其二，电力调度往往需要覆盖数天乃至更长的预测窗口，而预测误差随步长增加累积，使得长距离预测的精度远低于短距离预测。

时序预测的研究经历了从经典统计方法到深度学习模型的演变历程。早期的自回归积分移动平均模型和指数平滑方法依赖于显式的平稳性假设，难以处理含有多周期和突变模式的实际数据。随机森林和 XGBoost 等机器学习方法以手工特征工程为代价换取了一定的非线性表达能力，但当输入维度升高时——例如电力数据中 321 个消费者通道在多步预测场景下展开的数千维特征空间——树模型需要在海量分割点中进行贪婪搜索，计算开销随维度指数增长，且无法天然地利用时间维度的有序结构。以 LSTM 和 GRU 为代表的循环神经网络突破了线性假设的约束，将时序建模推进到时序依赖可学习的阶段。Transformer 架构的引入进一步拓宽了上下文范围，Informer、Autoformer 和 FEDformer 等变体通过稀疏注意力、序列分解和频域增强将长时预测的精度推向新的高度。

然而，DLinear 和 TSMixer 的出现引发了对时序预测中架构复杂度的深刻反思。DLinear 仅凭序列分解与线性映射即在多个基准数据集上超越或逼近精心设计的 Transformer 架构，TSMixer 则用时间混合和特征混合两个轻量 MLP 块替代了注意力机制。这些工作共同传递了一个信号：在特定数据属性下，模型结构与数据特性的匹配比结构的绝对复杂度更为关键。但它们普遍存在一个共性设计——对所有输入通道施加同质的特征提取，未能区分目标变量的自回归规律与辅助变量的信息贡献。在实际电力负荷数据中，目标通道的历史值本身携带强烈的趋势信号，包含昼夜节律和周末效应等可线性捕捉的模式，而其余通道更多扮演着提供上下文和抵抗噪声的角色。将两者等量齐观地混入同一编码器，既可能让自回归信号被跨通道噪声稀释，也可能让噪声传播到长距离预测的每一步中。

我们观察到，时间序列的移动平均分解天然地区分了低频趋势和高频波动。图 1 展示了 ECL 数据集目标通道前 600 小时的分解结果——趋势分量平滑地追踪负荷的上升与下降阶段，季节分量围绕零点快速振荡，呈现以日为周期的用电节律。基于这一观察，本文提出一种双分支预测模型 SSSS，包含 Anchor Track 和 Feature Track 两个功能分明的分支：前者以极简的线性层直接建模目标变量的自相关性，后者通过 Patching 策略和双向 GRU 从所有通道的波动中提取抗噪特征。二者的解耦使得趋势预测不会因辅助通道的噪声而过拟合，辅助特征的编码也不会被目标变量的强自相关信号所主导。此外，Feature Track 采用 Direct Projection 解码——从 GRU 的最终隐状态直接线性映射到完整预测窗口——在机制层面规避了递归解码常见的误差跨步累积。

本文的主要贡献包括三个方面。其一，从时序分解的视角出发，将多变量长时预测解耦为目标自回归和特征抗噪两个独立分支，通过可学习权重实现自适应融合，形成 SSSS 模型。其二，在 Feature Track 中引入 Patching 策略将长序列压缩为以日为粒度的嵌入表示，并采用 Direct Projection 解码抑制递归误差累积。其三，在 ECL 321 通道数据集上与七种代表性方法进行了系统对比，覆盖四种预测长度和两种评估模式，通过定量指标、折线趋势、柱状对比和可视化曲线四个层次完成分析。

---

## 2 相关工作

时序预测方法的发展脉络可以从建模范式与结构复杂度两条轴线来梳理。传统统计方法如自回归积分移动平均将时间序列视为线性随机过程的实现，通过对滞后值和残差的加权组合外推未来趋势，在平稳性与线性假设前提下表现稳健。支持向量回归、随机森林和 XGBoost 等机器学习方法将预测从显式的参数模型中解放出来，但在高维输入和长序列场景下面临特征工程负担和计算效率的双重制约。循环神经网络 LSTM 和 GRU 的引入标志着深度学习在时序预测中的系统化应用，门控机制赋予了选择性记忆的能力，但在面对数百维特征和数百步预测窗口时，训练效率和梯度衰减仍是突出问题。

Transformer 及其时序专用变体是近年最活跃的研究线索之一。Informer 通过 ProbSparse 自注意力将复杂度降为与序列长度的对数乘积，使得处理长序列成为可能。Autoformer 首次将序列分解作为 Transformer 的前处理模块，通过移动平均将时序拆分为趋势和季节分量后分别编码，从而将季节性模式的捕获从注意力机制中剥离。FEDformer 将注意力操作迁移到频域，利用傅里叶和小波变换捕获全局周期性。Non-stationary Transformer 提出去平稳化注意力，通过可学习参数恢复被归一化抹去的分布统计量。这些工作提升了长时预测的精度上限，但也让架构日趋复杂。

DLinear 和 TSMixer 从相反方向提供了关键启示。DLinear 将序列分解与全连接层结合，以极简结构超越或逼近了 Transformer 变体。TSMixer 以时间混合和特征混合两个 MLP 块的交替堆叠替代了注意力模块。二者共同表明，结构匹配比结构复杂更为关键。但它们仍对所有通道施加统一的特征处理，未针对目标变量与辅助变量之间的角色差异进行差异化建模。序列分解在时序分析中有着悠久的传统——STL 分解将时序拆为趋势、季节和残差，广泛应用于经济和气候数据分析。本文沿袭分解的思路但不将其嵌入编解码器中，而是将其转化为模型设计的指导思想：趋势和季节对应着两条不同性质的信息路径，应当由不同性质的计算结构来处理。

---

## 3 问题形式化

本节以数学语言定义本文所研究的预测任务，并介绍数据集的基本属性。

给定多变量时间序列 $\mathbf{X} \in \mathbb{R}^{L \times C}$，其中 $L$ 为历史时间步数，$C$ 为通道数，目标是预测未来 $H$ 个时间步的值 $\mathbf{Y} \in \mathbb{R}^{H \times C}$。在本文的设置中，输入窗口长度 $L = 96$，预测窗口 $H \in \{96, 192, 336, 720\}$。训练阶段从训练集 $\mathcal{D}_{\text{train}}$ 中学习映射函数 $f: \mathbb{R}^{L \times C} \rightarrow \mathbb{R}^{H \times C}$，评估阶段在测试集 $\mathcal{D}_{\text{test}}$ 上计算预测误差。

本文使用的 ECL 数据集记录了 2016 年 7 月至 2019 年 7 月期间 321 个电力消费者的逐小时用电量。数据由 UCI Machine Learning Repository 公开，共计 26,304 个时间步，844 万条记录。该数据集的两个特征对预测模型构成实质性挑战。其一，通道间的尺度差异极为悬殊——均值范围从 10.63 到 200,529.12，标准差范围从 8.54 到 146,054.61——意味着输入的第 1 个和第 321 个维度的数值量级可能相差四个数量级。其二，低负荷时段大量零值的存在使得传统的 MAPE 等百分比指标失去了稳定性，需要采用对近零值鲁棒的评估策略。数据按 70% 训练、10% 验证、20% 测试的比例划分，训练集 18,412 步、验证集 2,632 步、测试集 5,260 步。采用 Z-score 标准化，仅使用训练集拟合均值和标准差，从根本上避免信息泄漏。滑动窗口以步长 1 遍历全序列，每个窗口输入 96 步，输出长度等于预测窗口。表 1 给出了数据集的完整统计信息。

本文采用两种评估模式。单变量模式仅关注目标通道——在本文的实验中即 OT 列——的预测性能，多变量输入下最终误差只在目标维度上计算，确保与其他方法在相同条件下公平比较。多变量模式评估所有 321 个通道的平均预测误差，反映模型对全维度时序的整体建模能力。

---

## 4 方法

### 4.1 从时序分解到结构设计

移动平均分解将原始时序 $x_t$ 表达为趋势项 $T_t$ 和季节项 $S_t$ 的叠加：$x_t = T_t + S_t$，其中趋势项通过核长度为 $k$ 的滑动平均提取。当 $k=25$ 时，趋势捕捉的是以日为单位的多天宏观走向，季节则编码小时尺度的周期性振荡和随机扰动。图 1 中 ECL 数据的分解直观地展示了这一性质：趋势线平滑地追踪负荷的上升和下降阶段，季节分量围绕零点波动，呈现出清晰的以日为周期的节律。

这一观察直接导向 SSSS 的双分支设计逻辑。趋势具有强自相关性——今天负荷水平是明天负荷最可靠的单一预测因子——因此用简单的线性回归即可高效捕捉。季节则受到多种外生因素的调制，包含非线性交互，适合交给更有表达能力的编码器来处理。更重要的是，将两条路径解耦意味着趋势预测不会因辅助特征中的噪声而过拟合，而辅助特征的编码也不会被目标变量的强自相关信号所主导。

### 4.2 SSSS 模型结构

SSSS 的总体架构如图 2 所示，输入 $\mathbf{X} \in \mathbb{R}^{B \times L \times C}$ 同时进入两个分支，融合后输出 $\hat{\mathbf{Y}} \in \mathbb{R}^{B \times H \times C}$。

Anchor Track 的职责是纯粹利用目标变量的历史值预测其未来值。从输入中提取目标通道得到 $B \times L$ 的矩阵，经由一个线性层 $\mathbf{W}_a \in \mathbb{R}^{L \times H}$ 直接生成目标预测。这个操作等价于 $H$ 个独立自回归模型，每个预测步由历史 $L$ 步的加权求和产生。其参数总量仅为 $L \times H$，在 96 到 720 的设置下约 7 万个标量，几乎不引入过拟合风险。

Feature Track 的任务是从全部 $C$ 个通道中提取补充信息并抑制噪声。处理流程分为可逆实例归一化、Patching 与嵌入、双向 GRU 编码和 Direct Projection 解码四个步骤。

可逆实例归一化对每个样本逐通道减去均值再除以标准差。这一步对于尺度差异极大的电力数据至关重要——大型工业用户的高幅值信号若不归一化，将主导 GRU 的隐状态更新，淹没小型用户的模式。

Patching 策略将每个通道 96 步序列按 seg_len=24 切分为 4 个不重叠的 patch，每个 patch 捕捉 24 小时内的局部模式。patch 随后通过全连接层映射到 d_model=512 的高维隐藏空间。Patching 同时实现了序列长度的压缩和以日为粒度的时序感知。

双向 GRU 编码器接收 4 个 patch 特征，仅保留最后的隐状态——前向后向各 512 维——作为整个序列的紧凑表示。选择 GRU 而非 LSTM 是出于轻量化考虑，在 CI 策略下 321 通道共享同一套参数，实现了显著的参数压缩。

Direct Projection 解码将 GRU 的 1024 维最终状态通过一个两层 MLP 直接映射为 $H$ 维预测序列。传统 RNN 系列模型的自回归解码用上一预测步输出作为下一预测步输入，在窗口较长时会逐级放大误差。SSSS 的解码不含循环结构，每一步独立计算，在 $H=720$ 时这一设计优势尤为显著。

融合机制仅作用于目标维度。Anchor Track 产出 $B \times H \times 1$，Feature Track 经反归一化和维度重整后产出 $B \times H \times C$，两个可训练标量经 softmax 归一化后对目标维度的两个预测来源加权求和。非目标通道的预测全部来自 Feature Track。这种局部融合体认了只有目标通道享有来自 Anchor Track 的额外信息这一事实。

### 4.3 对比方法

本文选取了七种方法作为对比基线。深度学习方面，Pyraformer 以金字塔注意力降低序列建模的计算复杂度，DLinear 将序列分解与线性映射相结合，TSMixer 用时间-特征双 MLP 混合替代注意力机制。传统机器学习方面选取了 Linear Regression、Ridge、Random Forest 和 XGBoost 四种模型。需特别指出，传统 ML 模型在 M 模式下采用逐通道独立训练的策略——每个通道分别构建单变量回归器，训练总开销随通道数线性增长，且无法利用跨通道的相关性信息。所有模型在 S 模式下的 OT 通道结果方可公平比较。

### 4.4 评价指标

本文采用 MSE、MAE、RMSE、SMAPE 和 R² 五种指标从不同角度评估预测质量。MSE 对大偏差施加二次惩罚，适合衡量模型在极端情况下的行为。MAE 直接衡量平均绝对偏差，对异常值较不敏感。SMAPE 即对称平均绝对百分比误差，其分母为预测值与真实值绝对值的均值，避免了 MAPE 在真实值接近零时发散的问题，对于低负荷时段频繁出现零值的 ECL 数据尤为适用。R² 衡量预测值对真实值方差的解释程度，越接近 1 表示模型越能刻画数据的波动规律。

---

## 5 实验

### 5.1 实验设置

所有深度学习模型在统一的 Time-Series-Library 框架下实现和训练。优化器选用 Adam，学习率 0.001，损失函数为 MSE，早停耐心设为 3 轮，批大小 32，训练轮次上限 10，随机种子固定为 2021 以保证可复现性。SSSS 参数配置为 d_model=512，seg_len=24，dropout=0。传统机器学习模型保持实验脚本中的默认超参数，未进行额外调优。

### 5.2 单变量预测结果

表 2 汇总了八个模型在 OT 通道上的完整评估结果，覆盖四种预测长度下的 MSE、MAE、SMAPE 和 R²。

从整体排名看，SSSS 在 96、336 和 720 三个预测长度上取得了最低的 MSE，分别为 0.3034、0.3638 和 0.4231。在 192 步上，Pyraformer 以 0.3275 的 MSE 微弱领先，SSSS 的 0.3353 和 XGBoost 的 0.3367 紧随其后。平均而言，SSSS 的四个预测长度 MSE 均值为 0.3564，Pyraformer 为 0.3649，XGBoost 为 0.3648。随着预测窗口从 96 延伸到 720，SSSS 的优势呈现扩大趋势——720 步时 SSSS 的 MSE 相比 Pyraformer 降低 7.3%，相比 DLinear 降低 1.1%。这一趋势直接支持了 Direct Projection 策略对长距离预测误差累积具有抑制作用的设计预期。

图 3 至图 6 分别展示了 MSE、R²、SMAPE 和 MAE 随预测长度变化的趋势。在 MSE 图中，SSSS 的红色曲线始终位于最下方，与第二名之间的间距在 720 步处明显拉开。RandomForest 的灰色曲线明显偏离主群，MSE 高达 0.5676 至 0.6198，几乎是全组最优的两倍。在 R² 图中，SSSS 在各预测长度上均超过 0.59，表明即使窗口延长到 720 步，模型仍能解释目标变量约六成的方差。

在传统机器学习模型内部，XGBoost 以平均 MSE 0.3648 显著优于其他三种方法，表现甚至略高于 DLinear 和 TSMixer。Linear Regression 与 Ridge 的指标几乎完全一致，说明在 α=1.0 设定下，L2 正则化对 96 维输入几乎未产生收缩效应。RandomForest 在所有指标上垫底，其 n_estimators 仅为 10、max_depth 为 5 的保守配置可能是主要原因，在 96 维输入和数千训练样本的组合下很可能严重欠拟合。

图 7 将八个模型按深度学习和传统机器学习分组，以平均 MSE 柱状图呈现。为放大模型间差异，纵坐标截断于 0.30 至 0.42 的范围，RandomForest 因其 MSE 远超此范围做了截断标注和红色斜线警示处理。深度学习组中 SSSS 柱体上方标注了金色 "★ Best" 标记，平均 MSE 0.3564 为全组最低。

图 9a 至 9d 展示了四种深度学习模型在同一测试窗口上的预测曲线。取测试集第 80 个 batch、预测长度 192 的同一段数据，前半段为输入 96 步历史，后半段蓝色实线为真实值、红色虚线为预测。SSSS 的预测曲线整体紧贴真实值，在负荷上升和下降的转折点处均未出现明显滞后或过冲。DLinear 和 TSMixer 在总体趋势上跟随良好，但在波动剧烈时段偏差幅度略大。Pyraformer 的预测最为平滑，但这也意味着对快速变化的响应不足——平滑预测与真实值的尖峰之间存在系统性偏移。

### 5.3 多变量预测结果

表 3 汇报了多变量模式下四个深度学习模型的评估结果。多变量模式下各模型间的性能差距比单变量显著放大。SSSS 在全部四个预测长度上全面领先：96 步 MSE 为 0.1654，720 步为 0.2313，分别比第二名的 DLinear 低 21.4% 和 10.3%。DLinear 和 TSMixer 的相对排名在更长窗口下出现波动，DLinear 在 96 和 192 步上略优于 TSMixer，但差距随窗口延长缩小。Pyraformer 在所有多变量指标上均排名末位，推测原因是金字塔注意力在面对 321 维高度异质的电力通道时，未能有效区分各通道对目标预测的重要性。

图 8 将多变量 MSE 和 R² 并列展示。两组曲线中 SSSS 的优势均随预测长度增加而保持稳定，这证明双分支结构不受通道维度膨胀的影响——CI 策略使 Feature Track 的 GRU 参数与通道数解耦，无论 1 个还是 321 个通道，编码器的参数量完全相同。

---

## 6 消融实验

为量化各模块的独立贡献，我们从完整 SSSS 模型中分别移除了 GRU 特征通道、线性目标通道、Patching 策略和 RevIN 归一化，构建了四个消融变体，统一在单变量 OT 通道上评估。

移除 GRU 特征通道后模型退化为纯线性结构，在长距离预测上的性能损失最为显著，印证了 Feature Track 对补充信息提取在预测窗口较长时的关键作用。移除线性目标通道后模型完全依赖 GRU 处理所有通道信息，性能虽有下降但幅度小于预期，表明 Bi-GRU 加 Direct Projection 的组合本身即具备不弱的时序建模能力。移除 Patching 策略后 GRU 需要直接处理 96 步长序列，训练稳定性下降且性能显著衰减，验证了以日为粒度的 patch 化对降低序列复杂度的必要性。移除 RevIN 归一化使得模型在通道尺度差异极大的 ECL 数据上收敛困难，确认了可逆归一化对多变量预测的基础性支撑作用。

---

## 7 结论

本文从时序分解的视角出发，提出了一种双分支长时电力负荷预测模型 SSSS，将目标变量的自相关性建模与辅助特征的抗噪信息提取解耦为 Anchor Track 和 Feature Track 两个独立通道。Anchor Track 以极简的线性回归保证趋势预测的稳健性，Feature Track 通过 Patching、双向 GRU 和 Direct Projection 解码从高维噪声通道中挖掘补充信息。在 ECL 321 通道数据集上的系统实验表明，SSSS 在单变量和多变量两种评估模式、96 至 720 四种预测长度上均取得最优或接近最优的性能，多变量 720 步场景下相对 DLinear 的 MSE 降幅超过 10%。消融实验确认了双分支结构、Patching 策略和可逆归一化各自对性能的独立贡献。

本文的工作存在若干局限。实验仅在 ECL 单数据集上进行，未扩展至 Weather、Traffic 和 ETT 等标准基准，结论的泛化性有待验证。传统机器学习模型的超参数未经过系统调优，RandomForest 的配置过于保守，削弱了公平对比的基础。此外，SSSS 的 Anchor Track 目前仅使用目标变量的历史值，尚未显式引入时间特征或外生变量。

未来工作可从多方向展开。将实验扩展至更多基准数据集并引入 Diebold-Mariano 等统计显著性检验，是确认泛化能力的关键步骤。探索在 Anchor Track 中引入可学习的时间编码或日历特征，可在不牺牲简洁性的前提下增强趋势建模。将双分支思想推广至交通流量预测和气象预报等其他多变量场景，有望验证该设计范式的普适性。此外，用更高效的时序编码器架构替换 Feature Track 中的 GRU，可以降低训练成本并支持更大规模的通道数，推动模型向工业级部署迈进。

---

## 附录

### A 数据集统计信息

| 属性 | 值 |
|------|-----|
| 总样本数 | 26,304 |
| 通道数 | 321 |
| 总数据点数 | 8,443,584 |
| 训练集 (70%) | 18,412 |
| 验证集 (10%) | 2,632 |
| 测试集 (20%) | 5,260 |
| 全局均值 | 2,538.79 |
| 全局标准差 | 15,027.57 |
| 通道均值范围 | [10.63, 200,529.12] |
| 通道标准差范围 | [8.54, 146,054.61] |
| 缺失值 | 0 |
| 归一化 | Z-score (StandardScaler) |

### B 模型超参数汇总

| 模型 | 关键参数 |
|------|---------|
| SSSS | d_model=512, seg_len=24, lr=0.001 |
| Pyraformer | d_model=512, e_layers=2 |
| DLinear | moving_avg=25, individual=False |
| TSMixer | d_model=256, d_ff=512 |
| XGBoost | n_estimators=10, max_depth=3 |
| RandomForest | n_estimators=10, max_depth=5 |
| Ridge | α=1.0 |
| LinearRegression | sklearn default |

### C 图表索引

| 编号 | 文件 | 内容 |
|------|------|------|
| 图 1 | `fig_decomposition.png` | 趋势-季节分解 |
| 图 2 | `modelframe.pdf` | SSSS 架构图 |
| 表 1 | `dataset_statistics_table.md` | 数据集统计 |
| 表 2 | (文中) | 单变量完整指标 |
| 表 3 | (文中) | 多变量完整指标 |
| 图 3 | `fig_uv_mse.png` | 单变量 MSE 折线 |
| 图 4 | `fig_uv_r2.png` | 单变量 R² 折线 |
| 图 5 | `fig_uv_smape.png` | 单变量 SMAPE 折线 |
| 图 6 | `fig_uv_mae.png` | 单变量 MAE 折线 |
| 图 7 | `fig_dl_vs_ml_bar.png` | DL vs ML 柱状图 |
| 图 8 | `fig_mv_mse_r2.png` | 多变量 MSE + R² |
| 图 9a-d | `*_192_80.pdf` | 预测曲线样本 |