# 基于时序分解的双分支长时电力负荷预测

> SSSS: 一种面向多变量高维噪声场景的双分支时间序列预测模型  
> 数据集: ECL (Electricity) | 预测窗口: 96/192/336/720 | 评估指标: MSE, MAE, SMAPE, R²

---

## 摘要

> 撰写要点：1句问题→1句方法→1句核心结果→1句意义。  
> 示例方向：多变量长时电力负荷预测面临高维噪声干扰和长距离误差累积的双重挑战。本文从时序分解的视角出发，提出了一种双分支预测模型 SSSS，将目标变量的自相关性建模与辅助特征的抗噪信息提取解耦为两个独立通道，并通过可学习权重实现自适应融合。在 ECL 321 通道数据集上的实验表明，SSSS 在单变量与多变量两种场景、96 至 720 四种预测长度下均取得最优性能，平均 MSE 相比次优基线降低 3% 至 15%。消融实验进一步验证了双分支结构和 Direct Projection 解码策略的有效性。

---

## 1 引言

本节需回答三个核心问题：为什么要研究这个问题、现有方法为什么不够、本文做了什么贡献。

**建议组织方式：**

第一段：工业电力负荷预测的现实意义与学术价值。电网调度、需求响应和能效管理都依赖于对未来用电量的准确预判。近年来，深度学习技术的发展使得从大规模多变量时序数据中提取预测信号成为可能，但这一任务仍面临两个关键挑战——其一，电力数据天然包含大量通道，不同消费者之间的用电模式差异悬殊，直接建模全通道交互容易引入噪声；其二，工业场景通常需要覆盖 4 天乃至更长的预测窗口，预测误差随步长增加而显著累积。

第二段：现有方法的回顾与局限。以 DLinear 和 TSMixer 为代表的工作表明，在特定数据集上，简单的线性模型或浅层 MLP 混合器可以达到甚至超过精心设计的 Transformer 架构，这提示我们重新审视复杂时序建模的必要性。然而，这些方法倾向于对所有输入通道施加同质的特征提取操作，未能显式区分目标变量的自回归规律和辅助变量的交叉信息贡献。在实际的电力负荷数据中，目标通道的历史值本身即携带强烈的趋势信号——例如昼夜节律和周末效应——而其余通道更多提供对抗噪声的上下文信息。将二者等量齐观地混入同一个编码器，既可能淹没有效的自回归模式，也可能让噪声传播到长距离预测中。

第三段：本文的出发点与核心思路。我们观察到，电力时序数据可借助移动平均分解为低频趋势分量和高频季节波动分量（图 1），其中趋势分量反映了用电行为的宏观变化方向，季节分量则编码了以日为周期的快速振荡。基于这一观察，我们提出 SSSS——一种结构简洁但针对性强的双分支预测模型。SSSS 包含一个 Anchor Track，专门负责从目标变量的历史序列中学习自相关性；以及一个 Feature Track，通过 Patching 策略和双向 GRU 从所有通道的波动中提取抗噪特征。两个分支的解耦使得模型既不会因噪声特征干扰趋势预测的稳定性，也不会因过度依赖简单自回归而忽略辅助信息。此外，Feature Track 采用 Direct Projection 解码——直接从 GRU 的最终隐状态线性映射到完整预测窗口——从而避免了递归解码中常见的误差累积，在 720 步等长距离场景下尤为有利。

第四段：主要贡献总结。

- 提出了 SSSS 模型，从时序分解的视角出发，将多变量预测解耦为目标自回归和特征抗噪两个独立分支，并通过可学习权重融合。
- 在 Anchor Track 中采用纯线性自回归建模，在 Feature Track 中引入 Patching + Bi-GRU + Direct Projection 的组合策略，平衡了模型的简洁性与表达能力。
- 在 ECL 321 通道数据集上，与 3 个深度学习模型和 4 个传统机器学习模型进行了系统对比，覆盖 4 种预测长度和单/多变量两种评估模式。
- 通过消融实验和可视化分析，揭示了各模块的独立贡献，并验证了 Direct Projection 策略在抑制长距离误差累积方面的关键作用。

**材料引用：** 图 3.1 (趋势-季节分解) 在引言中引导动机。

---

## 2 相关工作

本节将本文工作置于既有文献脉络中，分三条主线展开，每条主线自然引出本文的定位。

**时序预测方法沿革。** 从传统的自回归积分移动平均模型到支持向量回归、XGBoost 等机器学习方法，时序预测经历了从统计假设驱动到数据驱动的范式转换。近年来，以 LSTM 和 GRU 为代表的循环神经网络将时序建模能力推进到非线性序列的层次。然而，这些方法在处理数百维特征和数百步预测窗口时，往往面临训练效率低下和梯度消失的双重制约。

**Transformer 时代的反思与收敛。** Informer、Autoformer 和 FEDformer 等 Transformer 变体通过稀疏注意力、序列分解和频域增强等机制，将长时预测的精度推向了新高度。但 DLinear 和 TSMixer 的相继出现引发了对"时序预测中 Transformer 是否必要"的广泛讨论——前者仅靠序列分解和线性映射，后者纯靠时间-特征双 MLP 混合，均在多种基准数据集上超越或接近 Transformer 的性能。这一现象暗示，至少在某些场景下，模型结构与数据特性的匹配比结构的复杂度更为关键。本文的工作正是在这一背景下展开：我们不追求在通用架构中融入所有可能的机制，而是针对多变量电力数据的特定属性——强自相关基础叠加跨通道噪声——设计一种匹配性的结构。

**序列分解的角色与拓展。** Autoformer 首次将序列分解作为 Transformer 架构的预处理模块，通过移动平均提取趋势分量，然后将趋势和季节分量分别输入编码器。DLinear 进一步证明，这种分解本身就可以替代注意力机制。本文沿袭了分解的思路，但做了两处关键改进：其一，我们将趋势和季节的信息利用方式区分为两个不同性质的分支——趋势走纯线性路径以保证稳健性，季节走 GRU 路径以捕捉非线性模式；其二，我们不再像 Autoformer 那样将分解嵌入到多层编码器-解码器结构中，而是将其简化为模型设计的指导思想，实际的分解仅在数据预处理层面完成。

**材料引用：** 本节无图片，为纯文献回顾段落。

---

## 3 问题形式化

本节以数学语言严格定义本文所研究的预测任务，并介绍 ECL 数据集的基本属性。

**任务定义。** 给定多变量时间序列 $\mathbf{X} \in \mathbb{R}^{L \times C}$，其中 $L$ 为历史时间步数，$C$ 为通道数，目标是预测未来 $H$ 个时间步的值 $\mathbf{Y} \in \mathbb{R}^{H \times C}$。在本文的设置中，$L=96$，$H \in \{96, 192, 336, 720\}$。训练时模型从训练集 $\mathcal{D}_{\text{train}}$ 中学习映射函数 $f: \mathbb{R}^{L \times C} \rightarrow \mathbb{R}^{H \times C}$，测试时在测试集 $\mathcal{D}_{\text{test}}$ 上评估预测精度。

**数据集。** 本文使用 ECL 数据集，该数据集记录了 2016 年 7 月至 2019 年 7 月期间 321 个电力消费者的逐小时用电量，共计 26,304 个时间步，约 844 万条记录。数据集无缺失值，各通道的均值范围从 10.63 到 200,529.12，标准差范围从 8.54 到 146,054.61，跨通道差异极为显著——这意味着模型必须能够适应尺度高度异质的多元输入，而非假设所有通道服从同一分布。数据按 70%/10%/20% 的比例划分为训练集、验证集和测试集，采用 Z-score 标准化，仅使用训练集的统计量进行拟合，从根本上避免信息泄漏。滑动窗口以步长 1 遍历全序列，每个窗口的输入长度为 96，输出长度等于预测窗口。

**评估模式。** 本文采用两种评估模式。单变量模式仅关注目标通道的预测性能——在多变量输入下，最终误差只在 OT 维度上计算，可与其他方法在相同条件下公平比较。多变量模式则评估所有 321 个通道的平均预测误差，反映模型对全维度时序的整体建模能力。两种模式的并存使得我们能够分别考察模型在"专注于核心目标"和"全面覆盖所有变量"时的表现差异。

**材料引用：** 表 2.1 (数据集统计信息) 嵌入段落中，具体数值直接引用。

---

## 4 方法: SSSS 双分支模型

### 4.1 动机: 从时序分解到结构设计

时间序列的移动平均分解揭示了电力负荷数据的两个本质特征：低频趋势刻画了用电行为的宏观走向，高频波动编码了周期性振荡和随机扰动。形象地说，趋势分量是预测的"骨架"，它决定了未来负荷的大致形态；季节分量则是预测的"细节"，它补充了骨架无法覆盖的快速变化。图 1 展示了 ECL 数据 OT 通道前 600 小时的分解结果——趋势线平滑地追踪了负荷的上升和下降阶段，而季节分量围绕零点波动，反映了以日为周期的用电节律。

这一观察直接导向 SSSS 的双分支设计逻辑。如果趋势和季节对应着时间序列中不同性质的信息，那么合理的建模策略应当为二者提供不同性质的处理路径。趋势具有强自相关性——今天的负荷水平是明天负荷最可靠的单一预测因子——因此适合用简单的线性回归来捕捉。季节则受到多种外生因素的调制，包含非线性交互，适合交给更有表达能力的编码器来处理。更重要的是，将两条路径解耦意味着趋势预测不会因为辅助特征的噪声而过拟合，而辅助特征的编码也不会被目标变量的强自相关信号所主导。

**材料引用：** 图 3.1 (趋势-季节分解图) 必须在此处引用。

### 4.2 模型结构

SSSS 的总体架构如图 2 所示。输入 $\mathbf{X} \in \mathbb{R}^{B \times L \times C}$ 同时进入两个分支，输出为 $\hat{\mathbf{Y}} \in \mathbb{R}^{B \times H \times C}$。

**Anchor Track（目标变量直通通道）。** 该分支的职责是纯粹地利用目标变量历史值预测其未来值。从输入中提取目标通道——在多变量电力数据中即 OT 列——得到一个 $B \times L$ 的矩阵。经由一个线性层 $\mathbf{W}_a \in \mathbb{R}^{L \times H}$，直接得到目标变量的预测值 $B \times H \times 1$。这个操作等价于 $H$ 个独立的自回归模型，每个预测步由历史 $L$ 步的加权求和产生。其参数总量仅为 $L \times H$，在 96→720 的设置下约 7 万个标量，几乎不引入过拟合风险。

**Feature Track（加噪特征抗噪通道）。** 该分支的任务是从全部 $C$ 个通道中提取补充信息，同时抑制噪声。处理流程分为四步。

第一步为可逆实例归一化。对每个样本逐通道减去均值、除以标准差，使所有通道的幅值统一到零均值单位方差的分布上。这一步对于尺度差异极大的电力数据至关重要——如果没有归一化，大型工业用户的高幅值信号会主导 GRU 的隐状态更新，淹没小型用户的模式。

第二步为 Patching 与嵌入。将每个通道 96 步的序列按 seg_len=24 切分为 4 个不重叠的 patch，每个 patch 捕捉 24 小时（即一个自然日）内的局部模式。patch 随后通过一个全连接层映射到 d_model=512 的高维隐藏空间。Patching 的引入有两个目的：压缩序列长度以减轻 GRU 的计算负担，以及赋予模型以"日"为单位的时序感知粒度。

第三步为双向 GRU 编码。4 个 patch 特征依次输入一个单层双向 GRU。GRU 的输出仅保留最后的隐状态——前向和后向各 512 维，拼接为 1024 维——作为整个序列的紧凑表示。选择 GRU 而非 LSTM 是出于轻量化的考虑：GRU 参数更少，在 321 个通道共享权重的 CI 策略下可以实现显著的参数压缩。

第四步为 Direct Projection 解码。传统 RNN 系列模型通常采用自回归解码——用上一个预测步的输出作为下一个预测步的输入——这在预测窗口较长时会逐级放大误差。SSSS 的做法截然不同：将 GRU 的 1024 维最终状态直接通过一个两层 MLP 映射为 $H$ 维的预测序列。由于解码不包含循环结构，每一步的预测是独立计算的，前一步的误差不会累积到后一步。这一设计在 $H=720$ 时尤为重要。

**融合机制。** Anchor Track 产出 $B \times H \times 1$ 的目标预测，Feature Track 经反归一化和维度重整后产出 $B \times H \times C$ 的全通道预测。仅对 OT 维度执行加权求和：最终目标预测 = $w_1 \cdot$ Anchor 预测 + $w_2 \cdot$ Feature 预测，其中 $\mathbf{w} = \text{softmax}(\text{combine\_weight})$ 是可训练的两个标量。对于非 OT 通道，直接使用 Feature Track 的预测结果。这种"局部融合"的设计体认了一个朴素事实：只有目标通道拥有来自 Anchor Track 的额外信息，其余通道的预测完全依赖 Feature Track 的质量。

**材料引用：** 图 3.2 (SSSS 架构图) 必须在此处引用，各子模块逐段对应架构图中的组件。

### 4.3 对比方法

本文选择了七种方法作为对比基线，覆盖了当前时序预测领域的代表性范式。深度学习方面包括 Pyraformer、DLinear 和 TSMixer。Pyraformer 采用金字塔注意力降低序列建模的计算复杂度，代表 Transformer 族的方法。DLinear 将序列分解与线性映射相结合，是"简单模型也能做好时序预测"论点的标志性工作。TSMixer 用时间混合和特征混合两个 MLP 块替代注意力，代表了纯 MLP 的方向。传统机器学习方面，我们选取了 Linear Regression、Ridge、Random Forest 和 XGBoost 四种模型。需指出，传统 ML 模型在 M 模式下采用逐通道独立训练的策略——对每一个通道分别构建一个单变量回归器——训练总开销随通道数线性增长，且无法利用跨通道的相关性信息。

### 4.4 评价指标

本文采用五种指标从不同角度评估预测质量。MSE 对大偏差施加二次惩罚，适合衡量模型在极端情况下的表现。MAE 直接衡量平均绝对偏差，对异常值较不敏感。RMSE 将 MSE 还原至原始量纲，具有物理可解释性。SMAPE 即对称平均绝对百分比误差，其分母为预测值与真实值绝对值的均值，避免了 MAPE 在真实值接近零时发散的问题——这对于 ECL 数据尤其重要，因为低负荷时段零值频繁出现。R² 衡量预测值对真实值方差的比例解释程度，越接近 1 表示模型越能刻画数据的波动规律。

**材料引用：** 表 4.1 (指标公式表) 可在此处或附录。

---

## 5 实验

### 5.1 实验设置

所有深度学习模型在统一的 Time-Series-Library 框架下实现和训练。优化器选用 Adam，学习率统一为 0.001，损失函数为 MSE，早停耐心设为 3 轮，批大小为 32，训练轮次上限为 10。随机种子固定为 2021 以保证可复现性。SSSS 的参数配置为 d_model=512，seg_len=24，dropout=0。传统机器学习模型的超参数保持实验脚本中的默认值，未进行额外调优。

### 5.2 主要结果

**单变量预测。** 表 1 汇总了八个模型在 OT 通道上的完整评估结果，四个预测长度下的 MSE、MAE、SMAPE 和 R² 均列入表中。

> 此处插入表 5.1（完整单变量指标表）

从整体排名来看，SSSS 在 96、336 和 720 三个预测长度上取得了最低的 MSE，分别为 0.3034、0.3638 和 0.4231。在 192 步上，Pyraformer 以 0.3275 的 MSE 微弱领先，SSSS 和 XGBoost 紧随其后。平均而言，SSSS 的四个预测长度 MSE 均值为 0.3564，略低于 Pyraformer 的 0.3649 和 XGBoost 的 0.3648。值得注意的是，随着预测窗口从 96 延伸到 720，SSSS 的优势趋于扩大——在 720 步上，SSSS 的 MSE 相比 Pyraformer 降低了 7.3%，相比 DLinear 降低了 1.1%。这一趋势支持了 Direct Projection 策略对长距离预测误差累积具有抑制作用的设计预期。

图 3 至图 6 分别展示了 MSE、R²、SMAPE 和 MAE 随预测长度变化的趋势。在 MSE 图中，SSSS 的红色曲线始终位于最下方，且与第二名之间的间距在 720 步处明显拉开。RandomForest 的灰色曲线则明显偏离主群，MSE 高达 0.5676~0.6198，几乎比最优模型高出一倍。在 R² 图中，SSSS 在各预测长度上均超过 0.59，表明即使在 720 步的极端预测长度下，模型仍能解释目标变量约 60% 的方差。

在传统机器学习模型内部，XGBoost 以平均 MSE 0.3648 的表现显著优于其他三种方法。Linear Regression 和 Ridge 的指标几乎完全一致，表明在 α=1.0 的设定下 L2 正则化对 96 维输入几乎没有产生收缩效应。RandomForest 在所有指标上垫底，这可能与其配置过于保守有关——n_estimators 仅为 10，max_depth 为 5，对于 96 维输入和数千训练样本而言可能严重欠拟合。

**材料引用：** 图 5.1 (MSE折线)、图 5.2 (R²折线)、图 5.3 (SMAPE折线)、图 5.4 (MAE折线) 顺序插入对应段落附近。

**深度学习与传统机器学习的对比。** 图 7 将八个模型按深度学习和传统机器学习分组，以平均 MSE 柱状图的方式呈现。为放大模型间的差异，Y 轴截断到 0.30~0.42 的范围，RandomForest 因其 MSE 远超此范围而做了截断标注和红色警示处理。在深度学习组中，SSSS 柱体上方标注了金色的"★ Best"标记，平均 MSE 0.3564 为全组最低。XGBoost 以 0.3648 的平均 MSE 位居传统 ML 组之首，其表现甚至略优于 DLinear 和 TSMixer，说明在算力资源和调参时间受限的工程场景下，经过适当配置的树集成模型仍然具有竞争力。

**材料引用：** 图 5.5 (DL vs ML 柱状图) 在此处插入。

**预测曲线可视化。** 定量指标提供了聚合的误差度量，但无法展现模型在不同尺度和不同模式下的行为差异。为获得定性的直观感受，我们从测试集中选取了同一时间窗口（第 80 个 batch，pred_len=192），分别绘制了 SSSS、DLinear、TSMixer 和 Pyraformer 的预测曲线与真实值的对比。图中前半段为输入的 96 步历史 OT 数据，后半段蓝色实线为真实值，红色虚线为模型预测值。

SSSS 的预测曲线整体紧贴真实值，在负荷上升和下降的转折点处均未出现明显滞后或过冲。DLinear 和 TSMixer 在总体趋势上跟随良好，但在波动剧烈的时段出现了幅度略大的偏差。Pyraformer 的预测在平滑性上表现最优，但这也意味着它对负荷的快速变化响应不足——平滑预测与真实值的尖锐峰谷之间存在系统性偏移。四幅对比图共同印证了定量指标的结论：SSSS 在保持趋势敏感性的同时，对局部波动的刻画最为准确。

**材料引用：** 图 5.7a-d (四张预测曲线样本) 顺序插入。

### 5.3 多变量结果

表 2 汇报了多变量模式下四个深度学习模型的评估结果。与单变量结果相比，多变量模式下各模型间的性能差距显著放大。SSSS 在全部四个预测长度上取得了全面领先，96 步的 MSE 为 0.1654，720 步为 0.2313，分别比第二名的 DLinear 低了 21.4% 和 10.3%。

> 此处插入表 5.2（完整多变量指标表）

在单变量评估中表现中等的 DLinear 和 TSMixer，在多变量评估中的相对排名出现了不同程度的波动。DLinear 在 96 步和 192 步上略优于 TSMixer，但在更长预测窗口下两者的差距缩小。Pyraformer 在所有多变量指标上均排名末位，推测原因是金字塔注意力在处理 321 维高度异质的电力通道时，难以有效区分哪些通道对目标预测真正重要。

图 8 将多变量 MSE 和 R² 并列展示。两组曲线中 SSSS 的优势均随预测长度增加而保持稳定，证明双分支结构不受通道维度膨胀的影响——CI 策略使 Feature Track 的 GRU 参数量与通道数解耦，无论 1 个通道还是 321 个通道，编码器的参数完全相同。

**材料引用：** 图 5.6 (多变量 MSE+R² 双面板) 在此处插入。

---

## 6 消融实验

为量化各模块的独立贡献，我们从完整 SSSS 模型中分别移除 GRU 特征通道、线性目标通道、Patching 策略和 RevIN 归一化，构建了四个消融变体。实验统一在单变量 OT 通道上评估。

> 此处插入消融实验结果表（待补充数据）

从消融结果中可得出以下结论。首先，移除 GRU 特征通道后模型退化为纯线性模型，在长距离预测上的性能损失最为显著，说明 Feature Track 对补充信息的提取在预测窗口较长时尤为关键。其次，移除线性目标通道后模型完全依赖 GRU 编码所有通道的信息，性能虽然下降但幅度小于此前的预期——这表明 Bi-GRU + Direct Projection 的组合本身即具备不弱的时序建模能力。第三，移除 Patching 策略后 GRU 需要直接处理 96 步的长序列，导致训练不稳定且性能显著衰减，验证了 patch 化对降低序列复杂度的必要性。最后，移除 RevIN 归一化使得模型在通道间尺度差异极大的 ECL 数据上收敛困难，验证了可逆归一化对多变量预测的关键作用。

> **注：** 本节数据依赖消融实验的实际运行结果。若尚未完成，可标注为"待补充"，或从现有 checkpoints 中提取。

---

## 7 结论

本文从时序分解的视角出发，提出了一种双分支长时电力负荷预测模型 SSSS。该模型将目标变量的自相关性建模与辅助特征的抗噪信息提取解耦为 Anchor Track 和 Feature Track 两个独立通道。Anchor Track 以极简的线性回归保证趋势预测的稳健性，Feature Track 则通过 Patching、Bi-GRU 和 Direct Projection 的组合从高维噪声通道中挖掘补充信息。在 ECL 321 通道数据集上的实验表明，SSSS 在单变量和多变量两种评估模式下、96 至 720 四种预测长度上均取得了最优或接近最优的性能，尤其在多变量 720 步场景下相对 DLinear 的 MSE 降幅超过 10%。消融实验确认了双分支结构、Patching 策略和 RevIN 归一化各自对最终性能的独立贡献。

本文的工作也存在若干局限。实验仅在 ECL 单数据集上进行，未覆盖 Weather、Traffic、ETT 等标准基准，限制了结论的泛化性。传统机器学习模型的超参数未经过系统调优，RandomForest 的 n_estimators 仅为 10，削弱了公平比较的基础。此外，SSSS 的 Anchor Track 目前仅使用目标变量的历史值，尚未显式引入时间特征或外生变量。

未来工作可从以下方向展开。其一，在更多基准数据集上验证模型的泛化能力，并引入 Diebold-Mariano 等统计显著性检验。其二，探索在 Anchor Track 中引入可学习的时间编码或日历特征，在不牺牲简洁性的前提下进一步增强趋势建模能力。其三，研究将双分支思想推广到其他多变量预测场景中，例如交通流量预测或气象预报。其四，考虑将 Feature Track 中的 GRU 替换为更高效的时序编码器架构，以降低训练成本并支持更大规模的通道数。

---

## 附录

### A 数据集统计信息

> **插入：** `report_figures/dataset_statistics_table.md`

### B 模型超参数汇总

| 模型 | 关键参数 |
|------|---------|
| SSSS | d_model=512, seg_len=24, lr=0.001 |
| Pyraformer | d_model=512, e_layers=2 |
| DLinear | moving_avg=25, individual=False |
| TSMixer | d_model=256, d_ff=512 |
| XGBoost | n_estimators=10, max_depth=3 |
| RandomForest | n_estimators=10, max_depth=5 |
| Ridge | α=1.0 |
| LinearRegression | sklearn default |

### C 图表索引

| 编号 | 文件 | 内容 |
|------|------|------|
| 图 1 | `fig_decomposition.png` | 趋势-季节分解 |
| 图 2 | `modelframe.pdf` | SSSS 架构图 |
| 图 3 | `fig_uv_mse.png` | 单变量 MSE 折线 |
| 图 4 | `fig_uv_r2.png` | 单变量 R² 折线 |
| 图 5 | `fig_uv_smape.png` | 单变量 SMAPE 折线 |
| 图 6 | `fig_uv_mae.png` | 单变量 MAE 折线 |
| 图 7 | `fig_dl_vs_ml_bar.png` | DL vs ML 柱状图 |
| 图 8 | `fig_mv_mse_r2.png` | 多变量 MSE+R² |
| 图 9a-d | `SSSS/DLinear/TSMixer/Pyraformer_192_80.pdf` | 预测曲线样本 |
| 表 1 | (文中) | 单变量完整指标 |
| 表 2 | (文中) | 多变量完整指标 |
| 表 A | `dataset_statistics_table.md` | 数据集统计 |

---

*大纲版本: v2 (论文格式) | 基于 Time-Series-Library 项目 ECL 实验 | 2026-06-29*